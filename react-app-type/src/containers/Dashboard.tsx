import DashboardViews from '../components/dashboard';
import { connect } from 'react-redux';
// import { Dispatch } from 'redux';


const mapStateToProps = () => {
    return {}
};

const mapDispatchToProps =  () => {
    return {}
};

export default connect(mapStateToProps, mapDispatchToProps)(DashboardViews);